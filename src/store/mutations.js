export default {
    SET_USER(state, userInfo) {
        state.userInfo = userInfo;
    },

    SET_ASKINFO(state, askInfo) {
        state.askInfo = askInfo;
    },

    SET_LIST(state, list) {
        state.list = list;
    },
}