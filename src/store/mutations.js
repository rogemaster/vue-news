export default {
    SET_NEWS(state, news) {
        state.news = news;
    },

    SET_JOBS(state, jobs) {
        state.jobs = jobs;
    },

    SET_ASKS(state, asks) {
        state.asks = asks;
    },

    SET_USER(state, userInfo) {
        state.userInfo = userInfo;
    },

    SET_ASKINFO(state, askInfo) {
        state.askInfo = askInfo;
    }
}